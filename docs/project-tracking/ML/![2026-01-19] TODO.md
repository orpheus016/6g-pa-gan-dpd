# TO-DO List:

## Datasets
- [✓] Plot the IQ datasets to visualize signal characteristics and deterministically decide if its 256-QAM or 64-QAM (fetch DPA_200MHz [DONE] and do comparison constellation plots) {jujur agak ragu dia ini datasetnya dah 256-QAM (apa) atau masih **64-QAM (dpa)** harusnya mah udah 256-QAM tp kita mesti validasi dulu} {**USE DPA_200MHz**}
- [ ] Compare OpenDPD PA dataset to our PA digital twin dataset to see how well the digital twin simulates real-world behavior, then choose which dataset to use for training (tbh its only in validation no PA in training loop iirc). We might be able to treat one of them as test set if needed (the digital twin). {**LOOK UP DATASET https://github.com/lab-emi/OpenDPD/tree/main/datasets/MyCustomPA or refer to [1] M. Beikmirza, Y. Shen, L. C. N. De Vreede, and M. S. Alavi, “A Wideband Energy-Efficient Multi-Mode CMOS Digital Transmitter,” IEEE J. Solid-State Circuits, vol. 58, no. 3, pp. 677–690, Mar. 2023, doi: 10.1109/JSSC.2022.3222028.**}
- [ ] Collect more labeled data for training and validation especially for 256-QAM (standard and advanced 5G) and 1024-QAM (6G) (if possible)

## Plot and Visualization
- [✓] Plot the datasets from IQ to Spectrum Analyzer plot to get ACPR, EVM, and NMSE value (check if the ipynb validation is valid or not because the dataset states that the spectrum analyzer plot has 5 channel look up OpenDPDv2) **LOOK UP utils/metrics.py and util.py ON OPENDPD**
- [✓] MATLAB / **Python** for plotting??? BUT OPENDPD USE MATLAB
- [ ] Compare our mentioned plot to OpenDPD papers (check OpenDPD main github repo for more papers related to this work)
- [ ] QAT Performance plot after training (EVM vs Output Power, ACPR vs Output Power, NMSE vs Output Power)

## Model Development
- [✓] Check if this architecture is legit and why this architecture is chosen compared to the TRES-Delta-GRU or RNN on OpenDPDv2 or SparseDPD. Compare to them and what are the tradeoffs [Comparison] (docs/comparison_docs/ML/ML-ARCHITECTURE-COMPARISON.md) [DONE]
- [✓] Compare current jupyter notebook to the backup to the proposed architecture
- [✓] Customize Wasserstein and GP according to DPD application or no? [DONE]
- [✓] Fix train.py to match ARCHITECTURE.md (memory depth M=3, phase-normalized features, no Tanh at output) [DONE]
- [✓] Implement PNTDNNGenerator with 24-dim phase-normalized features [DONE]
- [✓] FIX jupyter notebook to match train.py and ARCHITECTURE.md (memory depth M=3, phase-normalized features, no Tanh at output)
    - [✓] Check the best hyperparameters (learning rate, batch size, epochs) for training (OpenDPD epoch = 240 batch size = 64, initial lr = 5e-3 with reduceonplateau (! search what is this))
    - [✓] Compare spectral loss computation with metrics computation opendpd and also verify if the implementation is correct since the dataset is 5 channel while on previous notebook only one channel exists (USE DPA_200MHz)
    ---
    - [✓] Check the best lambda (weight) on spectral loss which one is the best for DPD training (i hope this can be determined without training first or ablation then) {**NOPE NEED TRAINING FIRST**}
    - [✓] Implement early stopping to prevent overfitting during training
    - [○] Thermal training check the flow again on train.py and the jupyter notebook {**TBA LATER**}
    - [✓] Is this already best practice?
    - [✓] Find a way how to track metrics during training (ACPR/ACLR?, EVM, NMSE) and if cant how to formulate MSE to be related to those metrics directly **LOOK UP utils/metrics.py and util.py ON OPENDPD**. {}
    - [○] Verify validation and implement plotting during training for EVM, ACPR, NMSE (numbers on per epoch, graph for final) {**ABLATION STUDY if ACLR Mediatek is needed**}
    - [✓] Verify dataset loading (CSV loader) matches train.py (ILA convention: u_pa=target, y_pa=input)
    - [✓] Verify model creation (param count should be 1,362)
    - [✓] Verify train step (ILA: no PA in loop, matches train.py exactly — WGAN-GP + GP + spectral loss)
- [✓] Is M and K seriously that low??? (M=5, K=3) check again the PA model order from OpenDPD papers and other related works and our requirement for pre-6G (C. Tarver for foundation)

--- 21-01-2026 ---
- [ ] Implement checkpointing and resume logic in Colab training notebook to avoid losing progress after 90 minutes {**TBA LATER**}
    - [ ] Save model weights, optimizer state, and epoch/counter at regular intervals (e.g., every N epochs or minutes).
    - [ ] On notebook restart, load the latest checkpoint and resume training from the saved state.
    - [ ] Use torch.save() to save a dict with model.state_dict(), optimizer.state_dict(), and epoch.
    - [ ] Use torch.load() to restore them.
    - [ ] Add logic at the start of your training loop to check for a checkpoint and load it if present.
- [ ] Update documentation (ARCHITECTURE.md) to reflect actual implementation in train.py and training_colab_v2.ipynb {**TBA LATER**}
- [ ] Enable ablation study on different loss function components to understand their impact on performance {**TBA LATER**}
    - [ ] Remove each loss component (L1, spectral, WGAN-GP) one at a time, measure degradation in EVM, ACPR, NMSE.
    - [ ] Add frequency-domain mask loss (fMask) if regulatory mask is violated despite good ACPR.
    - [ ] Add differentiable EVM loss if EVM specifically lags while ACPR is fine.
- [ ] Implement differentiable NMSE loss in spectral_loss.py to complement current proxy losses (power loss + L1) for better optimization towards RF metrics {**TBA LATER**}
    - [ ] Update SpectralLoss.forward to compute NMSE in dB between predicted and target signals
    - [ ] Keep power loss as regularization, but reduce its weight
    - [ ] Validate that NMSE loss improves training convergence and final metrics
    - [ ] Update documentation to reflect changes in loss function
- [ ] Implement longer sequence implementation with large batch size (e.g., [batch, seq_len, 2] with seq_len ≫ 2) to enable differentiable spectral losses (ACLR, EVM) that directly optimize for RF metrics—this is proven to yield much better real-world DPD (see Mediatek-PAN-DPD, OpenDPDv2, SparseDPD) {**TBA LATER**}
    - [ ] Rewrite dataloader to yield long, contiguous windows (not just random pairs). This may require careful handling of sequence boundaries and shuffling.
    - [ ] Adapt model to process sequences, not just single samples.
    - [ ] Tune hyperparameters for gradient stability with frequency-domain losses (see Mediatek-PAN-DPD, Section V).
    - [ ] Ablation study: Compare performance with/without differentiable spectral losses.
    - [ ] Monitor memory usage and training speed trade-offs.
- [ ] Add MAE loss function option to compare with MSE for generator training, as Mediatek-PAN-DPD uses MAE—this may improve convergence and final performance {**TBA LATER**}
    - [ ] Implement MAE loss in train_step for generator
    - [ ] Compare training curves and final metrics (EVM, ACPR, NMSE) between MSE and MAE
    - [ ] Analyze which loss yields better DPD performance in practice

---
- [✓] Check Mediatek Paper for more maths on the adversarial system for DPD
- [○] Explore different loss functions for the generator and discriminator to improve training stability and performance [Run training first. If you hit targets (EVM < -45 dB, ACPR < -62 dBc, NMSE < -42 dB), you're done. If not:
Ablation study: Remove each loss component, measure degradation
Add fMask: If regulatory mask is violated despite good ACPR
Add differentiable EVM: If EVM specifically lags while ACPR is fine]
- [ ] Ablation study on different components of the GAN architecture to understand their impact on performance, check knowledge/GAN folder for reference (not only for ML but also for RTL where the SPSA implementation is important to compare)
- [ ] Try different architectures (CNN, RNN, Transformer) and compare their performance on DPD tasks if time allows {**CHECK https://github.com/lab-emi/OpenDPD/tree/main/backbones for other NN models**}

### QUESTIONS:
? [✓] Why dont the other paper optimize on ACPR, ACLR, EVM, NMSE instantly but MSE first? is it because ACPR ETC is not differentiable or cant be determined on OFDM input signal? check this [Differentiable but cant only be acpr but the evm and nmse and l1 will be a variant of mse for fast convergence. acpr is the novelty (additional calculation tp jangan terlalu berat lambdanya di acpr klo evm aja udah cukup)] (knowledge/GAN/DIFFERENTIABLE-METRICS.md)
? [✓] Why dont other paper use GAN for DPD or maybe they do but not popular? [Answer+Counter] (docs/comparison_docs/ML/GAN-CHALLENGES-AND-COUNTER-DPD.md)
? [✓] Use MAE or MSE since on Mediatek Paper they use MAE? check this too
? [✓] Why dont OpenDPDv2 use phase normalization on wideband like sparseDPD? check this too
? [✓] Is 60dBc ACPR is even useful for 5G/6G standard? check this too [ANSWER: MINIMAL SEBENARNYA -48dBc UDH CUKUP BAGUS tapi emang DPD suka kesusahan maintain angka dibawah itu pas 200-400MHz keatas, more like -62dBc pas 200 biar pas 400 bisa -48 tp idk verify this] (knowledge/metrics/ACPR-REQUIREMENT-5G-6G.md)
? [✓] Why CWGANGP Works [Proof] (knowledge/GAN/PROOF-WHY-CWGANGP-WORKS.md)
? [ ] Whats the my_PA in opendpd dataset? is it the PA model or the measured data? check this too